const executeFlow = async (
  options = { runOnBackground: true, isTestRun: false }
) => {
  const { runOnBackground, isTestRun } = options;
  // TODO: Check whether a user allowed to run flows or not. E.g. cloud version.
};

export default executeFlow;
