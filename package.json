{
  "name": "@automatisch/root",
  "private": true,
  "scripts": {
    "start:web": "lerna run --scope @*/web start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "publishConfig": {
    "registry": "http://localhost:5000"
  }
}
